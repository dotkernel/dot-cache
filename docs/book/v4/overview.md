# Overview

`dot-cache` is a DotKernel component based on `symfony-cache` that enables caching.

Using the in-memory and/or filesystem cache adapters greatly improves overall application response times.